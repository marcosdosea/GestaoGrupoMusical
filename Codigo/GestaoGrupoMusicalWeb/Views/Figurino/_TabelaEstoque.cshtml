@model IEnumerable<GestaoGrupoMusicalWeb.Models.EstoqueDTOViewModel>

<tbody>
    @foreach (var item in Model)
    {
        <tr class="border-bottom border-danger border-opacity-75 p-3 align-middle">
            <td class="p-3">
                @Html.DisplayFor(modelItem => item.Tamanho)
            </td>
            <td class="p-3">
                @Html.DisplayFor(modelItem => item.Disponivel)
            </td>
            <td class="p-3">
                @Html.DisplayFor(modelItem => item.Entregues)
            </td>
            <td class="p-3">
                <a role="button" class="btn btn-secondary btn-sm mb-2 mb-lg-0" asp-controller="" asp-action=""><i class="fa-solid fa-pen-to-square"></i> Editar</a>
                <a role="button" class="btn btn-secondary btn-sm" asp-controller="" asp-action=""><i class="fa-solid fa-xmark"> </i> Excluir</a>
            </td>
        </tr>
    }
</tbody>

